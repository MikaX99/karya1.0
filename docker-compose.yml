services:
  web:
    build: .
    container_name: karyasistem_app
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
      - PORT=8080
      - HOSTNAME=0.0.0.0
    restart: always
